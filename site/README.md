# FlowSound Website

This is the static landing page for FlowSound.

## Cloudflare Pages

Use these settings:

- Framework preset: `None`
- Build command: leave empty
- Build output directory: `site`
- Production branch: `main`

Recommended custom domain:

```text
flowsound.youseminar.cn
```

The page supports English and Simplified Chinese. It defaults to English unless the browser language starts with `zh`; users can switch manually.
